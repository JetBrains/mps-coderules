configurations {
    logicTest
}

dependencies {
    logicTest 'org.objenesis:objenesis:2.1@jar'
    logicTest 'org.mockito:mockito-core:2.24.0@jar'
    logicTest 'net.bytebuddy:byte-buddy:1.10.17@jar'
}

task copyDependencies (type: Copy) {
  from configurations.logicTest
  into 'lib'
}
